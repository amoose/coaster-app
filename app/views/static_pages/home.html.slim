- if signed_in?
	h2
	 | Welcome back,&nbsp;
	 = current_user.name
	/ = render :partial => 'map'

	= render :partial => 'station', :object => @station
	= render :partial => 'schedule', :object => @trains
	= render :partial => 'map', :object => @json

	/ = debug @station.trains
	hr
	/ = gmaps4rails(@json)
- else
	h3
		| Not logged in.
		/ = debug @stations
